# Core dependencies
pywin32==306  # Windows API wrapper for window title access and system tray functionality
tkinter  # Built into Python, no version needed
# SQLite3 is built into Python, no additional package needed
pystray==0.19.5  # Cross-platform system tray functionality
Pillow==10.2.0  # Python Imaging Library for icon handling
